server:
  port: 8082

spring:
  data:
    mongodb:
      uri: ${MONGO_URI}
      database: ${DB_NAME}

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

management:
  endpoints:
    web:
      exposure:
        include: "*"