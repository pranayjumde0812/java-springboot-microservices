server:
  port: 8888

spring:
  application:
    name: configserver
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///home/admin1/JAVA/java-springboot-microservices/13-Ecom-MicroService-Setup-Sever-config/configserver/src/main/resources/config


  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

management:
  endpoints:
    web:
      exposure:
        include: "*"